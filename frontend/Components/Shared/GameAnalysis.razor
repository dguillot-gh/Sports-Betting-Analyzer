@using SportsBettingAnalyzer.Models
@using SportsBettingAnalyzer.Services
@using MudBlazor
@inject BallDontLieService StatsService
@inject PythonMLServiceClient MLService
@inject ISnackbar Snackbar
@using System.Text.RegularExpressions

<style>
    .rationale-container {
        font-family: 'Inter', 'Roboto', 'Segoe UI', sans-serif;
        font-size: 1rem;
        line-height: 1.8;
        color: #37474f;
    }
    .rationale-header {
        font-weight: 800;
        color: #1a237e;
        letter-spacing: 0.5px;
    }
    .rationale-container ul {
        padding-left: 1.25rem;
        margin-bottom: 0px;
        list-style-type: none;
    }
    .rationale-container li {
        margin-bottom: 0.8rem;
        position: relative;
    }
    .rationale-container li::before {
        content: "•";
        color: #3949ab;
        font-weight: 900;
        display: inline-block;
        width: 1rem;
        margin-left: -1.25rem;
        font-size: 1.3rem;
        vertical-align: middle;
    }
    .rationale-container b, .rationale-container strong {
        color: #d81b60;
        font-weight: 700;
        background: rgba(216, 27, 96, 0.04);
        padding: 0 2px;
        border-radius: 2px;
    }
    .rationale-container p {
        margin-bottom: 1rem;
    }
    
    .ai-insight-card {
        background: #fafafa !important;
        border: 1px solid #cfd8dc !important;
        box-shadow: 0 8px 32px rgba(0,0,0,0.08) !important;
        border-radius: 20px !important;
        padding: 0 !important;
    }
    
    .ai-header-gradient {
        background: linear-gradient(135deg, #1a237e 0%, #283593 100%);
        color: white;
        padding: 24px 32px;
        border-radius: 20px 20px 0 0;
    }
    
    .analysis-panel {
        border-radius: 12px !important;
        margin-bottom: 12px !important;
        overflow: hidden;
        border: 1px solid #eceff1 !important;
        box-shadow: 0 2px 8px rgba(0,0,0,0.04) !important;
    }
    
    .factor-badge {
        background: #fff9c4;
        border-left: 5px solid #fbc02d;
        color: #5d4037;
    }
</style>

<MudDialog>
    <DialogContent>
        <MudContainer MaxWidth="MaxWidth.Medium">
            <MudGrid Class="mb-4">
                <MudItem xs="12" Class="d-flex justify-center">
                    <MudText Typo="Typo.h5">@Event.AwayTeam vs @Event.HomeTeam</MudText>
                </MudItem>
                <MudItem xs="12" Class="d-flex justify-center">
                    <MudText Typo="Typo.caption">@Event.CommenceTime.ToLocalTime().ToString("f")</MudText>
                </MudItem>
            </MudGrid>

            @if (_isLoading)
            {
                <MudProgressLinear Color="Color.Primary" Indeterminate="true" Class="my-4" />
                <MudText Align="Align.Center">Fetching stats from BallDontLie...</MudText>
            }
            else if (!string.IsNullOrEmpty(_errorMessage))
            {
                 <MudAlert Severity="Severity.Error">@_errorMessage</MudAlert>
            }
            else if (_statsMissing)
            {
                <MudAlert Severity="Severity.Warning">Could not find stats for one or both teams.</MudAlert>
            }
            else
            {
                <MudTabs Elevation="2" Rounded="true" ApplyEffectsToContainer="true" PanelClass="pa-6" Class="mt-4">
                    <MudTabPanel Text="Matchup Analysis" Icon="@Icons.Material.Filled.Analytics">
                        <MudGrid>
                            <!-- Away Team -->
                            <MudItem xs="4" Class="d-flex flex-column align-center">
                                <MudText Typo="Typo.h6">@Event.AwayTeam</MudText>
                                <MudText Typo="Typo.subtitle1">@(_awayStats?.AveragePoints.ToString("F1")) PPG</MudText>
                                 <MudText Typo="Typo.caption">Allowed: @(_awayStats?.AveragePointsAllowed.ToString("F1"))</MudText>
                            </MudItem>

                            <!-- Comparison Labels -->
                            <MudItem xs="4" Class="d-flex flex-column align-center justify-center">
                                <MudText Typo="Typo.body2" Class="mb-1">Points Per Game</MudText>
                                <MudDivider Class="my-2" Width="100%" />
                                <MudText Typo="Typo.body2" Class="mb-1">Opp. PPG</MudText>
                                <MudDivider Class="my-2" Width="100%" />
                                 <MudText Typo="Typo.body2" Class="mb-1">Recent Form (Last 5)</MudText>
                            </MudItem>

                            <!-- Home Team -->
                            <MudItem xs="4" Class="d-flex flex-column align-center">
                                <MudText Typo="Typo.h6">@Event.HomeTeam</MudText>
                                <MudText Typo="Typo.subtitle1">@(_homeStats?.AveragePoints.ToString("F1")) PPG</MudText>
                                <MudText Typo="Typo.caption">Allowed: @(_homeStats?.AveragePointsAllowed.ToString("F1"))</MudText>
                            </MudItem>
                        </MudGrid>
                        
                        <!-- Form Comparison -->
                        <MudGrid Class="mt-4">
                             <MudItem xs="6" Class="d-flex justify-center gap-1">
                                @if (_awayStats != null) {
                                    @foreach(var res in _awayStats.RecentForm) {
                                        <MudChip T="string" Color="@(res == "W" ? Color.Success : Color.Error)" Size="Size.Small" Class="mx-0">@res</MudChip>
                                    }
                                }
                            </MudItem>
                            <MudItem xs="6" Class="d-flex justify-center gap-1">
                                 @if (_homeStats != null) {
                                    @foreach(var res in _homeStats.RecentForm) {
                                        <MudChip T="string" Color="@(res == "W" ? Color.Success : Color.Error)" Size="Size.Small" Class="mx-0">@res</MudChip>
                                    }
                                }
                            </MudItem>
                        </MudGrid>
                        
                        <!-- H2H Comparison -->
                        @if (_h2hGames.Any())
                        {
                            <MudText Typo="Typo.subtitle2" Class="mt-4 mb-2">Head to Head (Last 5)</MudText>
                            <MudTable Items="@_h2hGames" Dense="true" Hover="true" Striped="true" Elevation="0">
                                <HeaderContent>
                                    <MudTh>Date</MudTh>
                                    <MudTh>Matchup</MudTh>
                                    <MudTh>Score</MudTh>
                                </HeaderContent>
                                <RowTemplate>
                                     <MudTd DataLabel="Date">@DateTime.Parse(context.Date).ToString("yyyy-MM-dd")</MudTd>
                                     <MudTd DataLabel="Matchup">@context.VisitorTeam.Abbreviation @@ @context.HomeTeam.Abbreviation</MudTd>
                                     <MudTd DataLabel="Score">
                                         @if ((context.HomeTeam.Id == _homeId && context.HomeTeamScore > context.VisitorTeamScore) || 
                                              (context.VisitorTeam.Id == _homeId && context.VisitorTeamScore > context.HomeTeamScore))
                                         {
                                             <MudText Color="Color.Success">@context.VisitorTeamScore - @context.HomeTeamScore</MudText>
                                         }
                                         else if (_homeId.HasValue) 
                                         {
                                              <MudText Color="Color.Error">@context.VisitorTeamScore - @context.HomeTeamScore</MudText>
                                         }
                                         else
                                         {
                                              <MudText>@context.VisitorTeamScore - @context.HomeTeamScore</MudText>
                                         }
                                     </MudTd>
                                </RowTemplate>
                            </MudTable>
                        }
                        <!-- Top Performers (Last Game) -->
                        @if (_homeTopPlayers.Any() || _awayTopPlayers.Any())
                        {
                             <MudText Typo="Typo.subtitle2" Class="mt-4 mb-2">Top Performers (Last Game)</MudText>
                             <MudGrid>
                                <MudItem xs="6">
                                    @foreach(var p in _awayTopPlayers)
                                    {
                                        <MudText Typo="Typo.caption" Class="d-block">@p.Player.FirstName @p.Player.LastName: <b>@p.Pts</b> pts</MudText>
                                    }
                                </MudItem>
                                <MudItem xs="6">
                                    @foreach(var p in _homeTopPlayers)
                                    {
                                        <MudText Typo="Typo.caption" Class="d-block">@p.Player.FirstName @p.Player.LastName: <b>@p.Pts</b> pts</MudText>
                                    }
                                </MudItem>
                             </MudGrid>
                        }
                        
                        <!-- Value Analysis with Edge Meters -->
                        @if (_homeStats != null && _awayStats != null)
                        {
                            <MudPaper Outlined="true" Class="pa-4 mt-4">
                                <MudText Typo="Typo.h6" Class="mb-3">
                                    <MudIcon Icon="@Icons.Material.Filled.Speed" Class="mr-2" />
                                    Stat-Based Edge Analysis
                                </MudText>
                                
                                <MudGrid>
                                    <!-- Spread Edge Meter -->
                                    <MudItem xs="12" md="6">
                                        <div class="text-center">
                                            <MudText Typo="Typo.subtitle2" Class="mb-2">Spread Edge</MudText>
                                            <div style="position: relative; width: 150px; height: 80px; margin: 0 auto;">
                                                <!-- Edge Meter Arc -->
                                                <svg viewBox="0 0 100 50" style="width: 100%; height: 100%;">
                                                    <!-- Background arc -->
                                                    <path d="M 10 50 A 40 40 0 0 1 90 50" fill="none" stroke="#e0e0e0" stroke-width="8" stroke-linecap="round"/>
                                                    <!-- Value arc -->
                                                    <path d="M 10 50 A 40 40 0 0 1 90 50" fill="none" 
                                                          stroke="@GetEdgeMeterColor(_spreadEdge)" 
                                                          stroke-width="8" 
                                                          stroke-linecap="round"
                                                          stroke-dasharray="@GetEdgeDashArray(_spreadEdge)"
                                                          stroke-dashoffset="0"/>
                                                    <!-- Center indicator -->
                                                    <line x1="50" y1="50" x2="50" y2="15" stroke="#666" stroke-width="2" opacity="0.5"/>
                                                </svg>
                                                <div style="position: absolute; bottom: 5px; left: 50%; transform: translateX(-50%);">
                                                    <MudText Typo="Typo.h5" Color="@GetEdgeTextColor(_spreadEdge)">
                                                        @(_spreadEdge > 0 ? "+" : "")@_spreadEdge.ToString("F1")
                                                    </MudText>
                                                </div>
                                            </div>
                                            <MudText Typo="Typo.caption" Color="Color.Secondary">
                                                Line: @_bettingSpread.ToString("F1") | Proj: @_projectedSpread.ToString("F1")
                                            </MudText>
                                            <MudChip T="string" Size="Size.Small" Color="@GetEdgeChipColor(_spreadEdge)" Class="mt-1">
                                                @GetEdgeLabel(_spreadEdge)
                                            </MudChip>
                                        </div>
                                    </MudItem>

                                    <!-- Total Edge Meter -->
                                    <MudItem xs="12" md="6">
                                        <div class="text-center">
                                            <MudText Typo="Typo.subtitle2" Class="mb-2">Total Edge</MudText>
                                            <div style="position: relative; width: 150px; height: 80px; margin: 0 auto;">
                                                <!-- Edge Meter Arc -->
                                                <svg viewBox="0 0 100 50" style="width: 100%; height: 100%;">
                                                    <!-- Background arc -->
                                                    <path d="M 10 50 A 40 40 0 0 1 90 50" fill="none" stroke="#e0e0e0" stroke-width="8" stroke-linecap="round"/>
                                                    <!-- Value arc -->
                                                    <path d="M 10 50 A 40 40 0 0 1 90 50" fill="none" 
                                                          stroke="@GetEdgeMeterColor(_totalEdge)" 
                                                          stroke-width="8" 
                                                          stroke-linecap="round"
                                                          stroke-dasharray="@GetEdgeDashArray(_totalEdge)"
                                                          stroke-dashoffset="0"/>
                                                    <!-- Center indicator -->
                                                    <line x1="50" y1="50" x2="50" y2="15" stroke="#666" stroke-width="2" opacity="0.5"/>
                                                </svg>
                                                <div style="position: absolute; bottom: 5px; left: 50%; transform: translateX(-50%);">
                                                    <MudText Typo="Typo.h5" Color="@GetEdgeTextColor(_totalEdge)">
                                                        @(_totalEdge > 0 ? "+" : "")@_totalEdge.ToString("F1")
                                                    </MudText>
                                                </div>
                                            </div>
                                            <MudText Typo="Typo.caption" Color="Color.Secondary">
                                                O/U: @_bettingTotal.ToString("F1") | Proj: @_projectedTotal.ToString("F1")
                                            </MudText>
                                            <MudChip T="string" Size="Size.Small" Color="@GetEdgeChipColor(_totalEdge)" Class="mt-1">
                                                @(_totalEdge > 0 ? "OVER" : _totalEdge < 0 ? "UNDER" : "PUSH")
                                            </MudChip>
                                        </div>
                                    </MudItem>
                                </MudGrid>

                                <MudDivider Class="my-3" />
                                <MudText Typo="Typo.caption" Color="Color.Secondary" Align="Align.Center">
                                    Edge = Projected - Line. Positive edge suggests value opportunity.
                                </MudText>
                            </MudPaper>
                        }
                    </MudTabPanel>
                    
                    @if (EspnData != null && Sport != "basketball_ncaab")
                    {
                        <MudTabPanel Text="ESPN BPI" Icon="@Icons.Material.Filled.SportsScore">
                            <MudGrid>
                                <MudItem xs="12">
                                     <MudCard Elevation="3" Class="pa-4">
                                        <div class="d-flex justify-space-between mb-2">
                                            <MudText><b>Home Win Prob:</b> @(EspnData.HomeWinProb.ToString("P1"))</MudText>
                                            <MudText><b>Away Win Prob:</b> @(EspnData.AwayWinProb.ToString("P1"))</MudText>
                                        </div>
                                        <MudProgressLinear Color="@(EspnData.HomeWinProb > EspnData.AwayWinProb ? Color.Success : Color.Error)" Value="@(EspnData.HomeWinProb * 100)" Size="Size.Large" Class="mb-4" />
                                        
                                        <MudSimpleTable Style="overflow-x: auto;">
                                            <thead>
                                                <tr>
                                                    <th>Metric</th>
                                                    <th>Value</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <tr>
                                                    <td>Predicted Spread</td>
                                                    <td>@EspnData.Spread</td>
                                                </tr>
                                                <tr>
                                                    <td>Predicted Total</td>
                                                    <td>@EspnData.OverUnder</td>
                                                </tr>
                                            </tbody>
                                        </MudSimpleTable>
                                    </MudCard>
                                </MudItem>
                            </MudGrid>
                        </MudTabPanel>
                    }

                    @if (Sport == "basketball_nba")
                    {
                        <MudTabPanel Text="Neural Network" Icon="@Icons.Material.Filled.Memory">
                            @if (_isEnsembleLoading)
                            {
                                <div class="d-flex flex-column align-center pa-8">
                                    <MudProgressCircular Color="Color.Primary" Indeterminate="true" Class="mb-4" />
                                    <MudText>Running Neural Network & XGBoost Ensemble...</MudText>
                                </div>
                            }
                            else if (_ensembleResult != null)
                            {
                                <MudAlert Severity="Severity.Info" Class="mb-4">
                                    This analysis combines kyleskom's pre-trained XGBoost model with a Neural Network "second opinion".
                                </MudAlert>

                                <MudGrid>
                                    <MudItem xs="12" md="6">
                                        <MudCard Elevation="4" Class="mud-theme-primary pa-4">
                                            <MudText Typo="Typo.h6">XGBoost (Primary)</MudText>
                                            <MudDivider Class="my-2" Light="true" />
                                            <MudText Typo="Typo.h4" Align="Align.Center" Class="my-4">
                                                @(_ensembleResult.HomeWinProbability.ToString("P1"))
                                            </MudText>
                                            <MudText Align="Align.Center">Home Win Prob</MudText>
                                            <div class="d-flex justify-space-between mt-4">
                                                <MudText Typo="Typo.caption">Confidence: @(_ensembleResult.Confidence.ToString("P0"))</MudText>
                                                <MudText Typo="Typo.caption">Winner: @_ensembleResult.PredictedWinner</MudText>
                                            </div>
                                        </MudCard>
                                    </MudItem>

                                    <MudItem xs="12" md="6">
                                        <MudCard Elevation="4" Class="mud-theme-secondary pa-4">
                                            <MudText Typo="Typo.h6">Neural Network (2nd Opinion)</MudText>
                                            <MudDivider Class="my-2" Light="true" />
                                            <MudText Typo="Typo.h4" Align="Align.Center" Class="my-4">
                                                @(_ensembleResult.NnHomeWinProbability?.ToString("P1") ?? "N/A")
                                            </MudText>
                                            <MudText Align="Align.Center">NN Win Prob</MudText>
                                        </MudCard>
                                    </MudItem>
                                    <!-- Model Agreement Alert -->
                                    @if (_ensembleResult.NnHomeWinProbability.HasValue)
                                    {
                                        <MudItem xs="12">
                                            @{
                                                var diff = _ensembleResult.NnHomeWinProbability.Value - _ensembleResult.HomeWinProbability;
                                                var agreement = Math.Abs(diff) < 0.05 ? "Strong Agreement" : (Math.Abs(diff) < 0.15 ? "Moderate Agreement" : "Model Discrepancy");
                                                var alertSeverity = Math.Abs(diff) < 0.05 ? Severity.Success : (Math.Abs(diff) < 0.15 ? Severity.Info : Severity.Warning);
                                            }
                                            <MudAlert Severity="@alertSeverity" Variant="Variant.Filled" Class="mt-2 text-white">
                                                <div class="d-flex align-center gap-2">
                                                    <MudIcon Icon="@(alertSeverity == Severity.Success ? Icons.Material.Filled.CheckCircle : Icons.Material.Filled.Info)" />
                                                    <div>
                                                        <b>@agreement:</b> The models @(Math.Abs(diff) < 0.05 ? "highly" : "") agree on this matchup. 
                                                        Win Prob Difference: @(Math.Abs(diff) * 100).ToString("F1")%
                                                    </div>
                                                </div>
                                            </MudAlert>
                                        </MudItem>
                                    }

                                    <!-- Betting Recommendations -->
                                    <MudItem xs="12">
                                        <MudPaper Outlined="true" Class="pa-4 mt-2" Style="background: rgba(0,0,0,0.02); border-left: 6px solid #ff9800;">
                                            <div class="d-flex align-center mb-2">
                                                <MudIcon Icon="@Icons.Material.Filled.Recommend" Color="Color.Warning" Class="mr-2" />
                                                <MudText Typo="Typo.subtitle1"><b>Betting Guidance (Ensemble-Backed)</b></MudText>
                                            </div>
                                            <MudGrid>
                                                <MudItem xs="12" sm="6">
                                                    <div class="pa-2 rounded" style="background: rgba(0,0,0,0.05)">
                                                        <MudText Typo="Typo.caption" Color="Color.Secondary">Expected Value (EV)</MudText>
                                                        <div class="d-flex justify-space-between mt-1">
                                                            <MudText Typo="Typo.body2">Home:</MudText>
                                                            <MudText Typo="Typo.body1" Color="@(_ensembleResult.HomeEv > 0.01 ? Color.Success : Color.Default)">
                                                                <b>@(_ensembleResult.HomeEv?.ToString("P1") ?? "N/A")</b>
                                                            </MudText>
                                                        </div>
                                                        <div class="d-flex justify-space-between">
                                                            <MudText Typo="Typo.body2">Away:</MudText>
                                                            <MudText Typo="Typo.body1" Color="@(_ensembleResult.AwayEv > 0.01 ? Color.Success : Color.Default)">
                                                                <b>@(_ensembleResult.AwayEv?.ToString("P1") ?? "N/A")</b>
                                                            </MudText>
                                                        </div>
                                                    </div>
                                                </MudItem>
                                                <MudItem xs="12" sm="6">
                                                    <div class="pa-2 rounded" style="background: rgba(0,0,0,0.05)">
                                                        <MudText Typo="Typo.caption" Color="Color.Secondary">Kelly Criterion (1% Cap)</MudText>
                                                        <div class="d-flex justify-space-between mt-1">
                                                            <MudText Typo="Typo.body2">Home Stake:</MudText>
                                                            <MudText Typo="Typo.body1"><b>@(_ensembleResult.HomeKelly?.ToString("P2") ?? "0.00%")</b></MudText>
                                                        </div>
                                                        <div class="d-flex justify-space-between">
                                                            <MudText Typo="Typo.body2">Away Stake:</MudText>
                                                            <MudText Typo="Typo.body1"><b>@(_ensembleResult.AwayKelly?.ToString("P2") ?? "0.00%")</b></MudText>
                                                        </div>
                                                    </div>
                                                </MudItem>
                                            </MudGrid>
                                        </MudPaper>
                                    </MudItem>
                                </MudGrid>
                            }
                        </MudTabPanel>
                    }
                    else if (Sport == "basketball_ncaab")
                    {
                        <MudTabPanel Text="Decision Matrix" Icon="@Icons.Material.Filled.Rule">
                            @if (_isEnsembleLoading)
                            {
                                <div class="d-flex flex-column align-center pa-8">
                                    <MudProgressCircular Color="Color.Primary" Indeterminate="true" Class="mb-4" />
                                    <MudText>Aggregating Model Predictions...</MudText>
                                </div>
                            }
                            else if (_ncaabComparison != null)
                            {
                                var simple = _ncaabComparison.GetSimpleResult();
                                var xgb = _ncaabComparison.GetXgbResult();
                                var espn = _ncaabComparison.GetEspnResult();
                                
                                <MudGrid>
                                    <MudItem xs="12">
                                        <MudAlert Severity="Severity.Info" Variant="Variant.Outlined" Class="mb-4">
                                            Decision Matrix consolidates Simple, XGBoost v2, and ESPN BPI models.
                                        </MudAlert>
                                    </MudItem>
                                    
                                    <MudItem xs="12">
                                        <MudSimpleTable Hover="true" Striped="true" Elevation="4">
                                            <thead>
                                                <tr>
                                                    <th>Model</th>
                                                    <th>Predicted Winner</th>
                                                    <th>Win Prob</th>
                                                    <th>Proj. Total</th>
                                                    <th>Confidence</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                @if (simple != null)
                                                {
                                                    <tr>
                                                        <td><MudChip T="string" Size="Size.Small" Color="Color.Default">Heuristic (Simple)</MudChip></td>
                                                        <td><b>@simple.Winner</b></td>
                                                        <td>@simple.WinProb.ToString("P1")</td>
                                                        <td>@simple.Total.ToString("F1")</td>
                                                        <td>-</td>
                                                    </tr>
                                                }
                                                @if (xgb != null)
                                                {
                                                    <tr style="background: rgba(var(--mud-palette-primary-rgb), 0.05)">
                                                        <td><MudChip T="string" Size="Size.Small" Color="Color.Primary">XGBoost v2 (Apex)</MudChip></td>
                                                        <td><b>@xgb.Winner</b></td>
                                                        <td>@xgb.WinProb.ToString("P1")</td>
                                                        <td>@xgb.Total.ToString("F1")</td>
                                                        <td>High</td>
                                                    </tr>
                                                }
                                                @if (espn != null)
                                                {
                                                    <tr>
                                                        <td><MudChip T="string" Size="Size.Small" Color="Color.Info" Icon="@Icons.Material.Filled.Radio">ESPN BPI</MudChip></td>
                                                        <td><b>@espn.Winner</b></td>
                                                        <td>@espn.WinProb.ToString("P1")</td>
                                                        <td>@((espn.TotalOverProb * 100).ToString("F0"))% Over</td>
                                                        <td>-</td>
                                                    </tr>
                                                }
                                            </tbody>
                                        </MudSimpleTable>
                                    </MudItem>

                                    <!-- Consensus Card -->
                                    <MudItem xs="12" Class="mt-2">
                                        @{
                                            var votes = new Dictionary<string, int>();
                                            if (simple != null) votes[simple.Winner] = votes.GetValueOrDefault(simple.Winner) + 1;
                                            if (xgb != null) votes[xgb.Winner] = votes.GetValueOrDefault(xgb.Winner) + 1;
                                            if (espn != null && !string.IsNullOrEmpty(espn.Winner)) 
                                                votes[espn.Winner] = votes.GetValueOrDefault(espn.Winner) + 1;

                                            var winnerVote = votes.OrderByDescending(v => v.Value).FirstOrDefault();
                                            string consensusWinner = winnerVote.Value >= 2 ? winnerVote.Key : "Mixed Opinion";
                                            int winCount = winnerVote.Value;
                                        }
                                        <MudPaper Elevation="4" Class="pa-6 rounded-lg mud-theme-dark" Style="background: linear-gradient(135deg, #1a237e 0%, #0d47a1 100%);">
                                            <div class="d-flex justify-space-between align-center">
                                                <div>
                                                    <MudText Typo="Typo.h6" Class="text-white opacity-70">CONSENSUS PICK</MudText>
                                                    <MudText Typo="Typo.h3" Class="text-white" Style="font-weight: 900;">@consensusWinner.ToUpper()</MudText>
                                                </div>
                                                <div class="text-right">
                                                    <MudText Typo="Typo.body2" Class="text-white opacity-70">CONFIDENCE</MudText>
                                                    <MudText Typo="Typo.h4" Color="@(winCount == 3 ? Color.Success : (winCount == 2 ? Color.Warning : Color.Default))">
                                                        @(winCount == 3 ? "HIGH" : (winCount == 2 ? "MED" : "LOW"))
                                                    </MudText>
                                                </div>
                                            </div>
                                        </MudPaper>
                                    </MudItem>
                                </MudGrid>
                            }
                            else
                            {
                                <MudAlert Severity="Severity.Warning">No comparison data available for this matchup.</MudAlert>
                            }
                        </MudTabPanel>
                    }
                    
                    <MudTabPanel Text="AI Insight" Icon="@Icons.Material.Filled.SmartToy" OnClick="LoadAiAnalysis">
                        @if (_isAiLoading)
                        {
                             <div class="d-flex flex-column align-center pa-8">
                                <MudProgressCircular Color="Color.Tertiary" Indeterminate="true" Class="mb-4" />
                                <MudText>Generating Gemini 1.5 Analysis...</MudText>
                            </div>
                        }
                        else if (_aiAnalysis != null)
                        {
                            <!-- XGBoost / ML Engine Display -->
                            @if (_aiAnalysis.Engines != null && _aiAnalysis.Engines.ContainsKey("XGBoost (ML)") 
                                 && _aiAnalysis.Engines["XGBoost (ML)"] is AiEnginePrediction xgb)
                            {
                                var prob = xgb.HomeWinProb;
                                var desc = xgb.Description;
                                
                                <MudCard Elevation="4" Class="pa-4 mb-4 mud-theme-primary">
                                    <div class="d-flex justify-space-between align-center">
                                        <div>
                                            <MudText Typo="Typo.h6">XGBoost Prediction</MudText>
                                            <MudText Typo="Typo.caption">@desc</MudText>
                                        </div>
                                        <MudChip T="string" Color="@(prob > 0.5 ? Color.Success : Color.Error)">
                                            Home Win: @prob.ToString("P1")
                                        </MudChip>
                                    </div>
                                    <MudProgressLinear Color="@(prob > 0.5 ? Color.Success : Color.Error)" Value="@(prob * 100)" Size="Size.Large" Class="mt-2" />
                                    
                                    @if (xgb.Explanation != null && xgb.Explanation.Value.TryGetProperty("top_features", out var topFeatures))
                                    {
                                        <MudText Typo="Typo.subtitle2" Class="mt-3 mb-1">Top 5 Influential Factors (SHAP)</MudText>
                                        <MudList T="string" Dense="true">
                                            @foreach (var feature in topFeatures.EnumerateArray())
                                            {
                                                var name = feature.GetProperty("feature").GetString();
                                                var impact = feature.GetProperty("impact").GetString(); // "Pos" or "Neg"
                                                var val = feature.GetProperty("shap_value").GetDouble();
                                                var color = impact == "Pos" ? Color.Success : Color.Error;
                                                var icon = impact == "Pos" ? Icons.Material.Filled.ArrowUpward : Icons.Material.Filled.ArrowDownward;
                                                
                                                <MudListItem Icon="@icon" IconColor="@color">
                                                    <div class="d-flex justify-space-between">
                                                        <MudText Typo="Typo.body2">@name</MudText>
                                                        <MudText Typo="Typo.caption" Color="@color">@(val > 0 ? "+" : "")@val.ToString("F3")</MudText>
                                                    </div>
                                                </MudListItem>
                                            }
                                        </MudList>
                                    }
                                </MudCard>
                            }

                            <!-- Gemini LLM Insight -->
                            @if (_aiAnalysis.LlmInsight != null)
                            {
                                <div class="ai-insight-card mb-4">
                                    <div class="ai-header-gradient d-flex justify-space-between align-center">
                                         <div>
                                             <MudText Typo="Typo.h4" Style="font-weight: 900; letter-spacing: 2px; text-shadow: 2px 2px 4px rgba(0,0,0,0.2);">AI PREDICTION</MudText>
                                             <MudText Typo="Typo.h5" Style="font-weight: 400; opacity: 0.9;">@(_aiAnalysis.LlmInsight.Winner?.ToUpper() ?? "UNKNOWN")</MudText>
                                         </div>
                                         <div class="text-right">
                                             <MudText Typo="Typo.caption" Style="text-transform: uppercase; font-weight: 700;">Model Confidence</MudText>
                                             <MudText Typo="Typo.h3" Style="font-weight: 900;">@_aiAnalysis.LlmInsight.Confidence<span style="font-size: 1.5rem">%</span></MudText>
                                         </div>
                                    </div>
                                    
                                    <div class="pa-8">
                                        <div class="factor-badge d-flex align-center mb-6 pa-5 rounded-lg">
                                            <MudIcon Icon="@Icons.Material.Filled.Whatshot" Size="Size.Large" Class="mr-4" Style="color: #f57f17;" />
                                            <div>
                                                <MudText Typo="Typo.caption" Style="font-weight: 900; color: #795548; text-transform: uppercase;">Primary Market Driver</MudText>
                                                <MudText Typo="Typo.h6" Style="font-weight: 800;">@_aiAnalysis.LlmInsight.KeyFactor</MudText>
                                            </div>
                                        </div>

                                        <MudText Typo="Typo.subtitle2" Class="mb-3 rationale-header">DEEP ANALYSIS REPORT</MudText>
                                        
                                        @{
                                            var analysisSections = SplitRationale(_aiAnalysis.LlmInsight.Rationale);
                                            var expandedIndex = 0;
                                        }

                                        <MudExpansionPanels MultiExpansion="false" Elevation="0">
                                            @foreach (var item in analysisSections)
                                            {
                                                expandedIndex++;
                                                <MudExpansionPanel Class="analysis-panel" IsExpanded="@(expandedIndex == 1)">
                                                    <TitleContent>
                                                        <div class="d-flex align-center">
                                                            <MudIcon Icon="@GetSectionIcon(item.Title)" Class="mr-3" Color="Color.Primary" />
                                                            <MudText Typo="Typo.subtitle1"><b>@item.Title</b></MudText>
                                                        </div>
                                                    </TitleContent>
                                                    <ChildContent>
                                                        <div class="rationale-container pa-4">
                                                            @((MarkupString)CleanseRationale(item.Content))
                                                        </div>
                                                    </ChildContent>
                                                </MudExpansionPanel>
                                            }
                                        </MudExpansionPanels>
                                        
                                        <div class="mt-8 pa-4 rounded-xl d-flex align-center justify-center" style="background: #eceff1; border: 1px solid #cfd8dc;">
                                            <MudIcon Icon="@Icons.Material.Filled.Shield" Size="Size.Small" Class="mr-3" Style="color: #546e7a;" />
                                            <MudText Typo="Typo.caption" Style="color: #546e7a; font-weight: 600;">
                                                Advanced Gemini 1.5 Flash Reasoning Engine • Verbatim Grounding Active
                                            </MudText>
                                        </div>
                                    </div>
                                </div>
                            }
                            
                            <MudExpansionPanel Text="Raw Engine Data">
                                <pre>@System.Text.Json.JsonSerializer.Serialize(_aiAnalysis.Engines, new System.Text.Json.JsonSerializerOptions { WriteIndented = true })</pre>
                            </MudExpansionPanel>
                        }
                    </MudTabPanel>

                </MudTabs>
            }
            
            <MudExpansionPanels Class="mt-4">
                <MudExpansionPanel Text="Betting Odds Details">
                    <MudGrid>
                        @foreach(var book in Event.Bookmakers)
                        {
                            <MudItem xs="12" sm="6">
                                <MudCard Outlined="true" Class="pa-2">
                                     <MudText Typo="Typo.subtitle2">@book.Title</MudText>
                                     @foreach(var market in book.Markets)
                                     {
                                         <div class="d-flex justify-space-between mt-1">
                                             <MudText Typo="Typo.caption">@FormatMarketKey(market.Key)</MudText>
                                             <div class="d-flex gap-2">
                                                 @foreach(var outcome in market.Outcomes)
                                                 {
                                                     <MudText Typo="Typo.caption">
                                                         @outcome.Name: <b>@FormatPrice(outcome.Price)</b> 
                                                         @(outcome.Point.HasValue ? $"({(outcome.Point>0?"+":"")}{outcome.Point})" : "")
                                                     </MudText>
                                                 }
                                             </div>
                                         </div>
                                     }
                                </MudCard>
                            </MudItem>
                        }
                    </MudGrid>
                </MudExpansionPanel>
            </MudExpansionPanels>

        </MudContainer>
    </DialogContent>
    <DialogActions>
        <MudButton Variant="Variant.Filled" Color="Color.Success" StartIcon="@Icons.Material.Filled.Save" OnClick="SaveToCache" Disabled="@_isSaving">
            @if (_isSaving)
            {
                <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" />
            }
            Save to Daily Cache
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] dynamic DialogInstance { get; set; }
    [Parameter] public OddsEvent Event { get; set; }
    [Parameter] public string Sport { get; set; } = "basketball_nba";
    [Parameter] public EspnGame? EspnData { get; set; }


    private TeamSeasonStats? _homeStats;
    private TeamSeasonStats? _awayStats;
    private List<BallDontLieGame> _h2hGames = new();
    private bool _isLoading = true;
    private bool _statsMissing = false;
    private string? _errorMessage;
    private int? _homeId;
    private int? _awayId;
    
    // Advanced Stats
    private List<BallDontLiePlayerStat> _homeTopPlayers = new();
    private List<BallDontLiePlayerStat> _awayTopPlayers = new();
    private double _projectedSpread;
    private double _projectedTotal;
    private double _bettingSpread;
    private double _bettingTotal;
    private double _spreadEdge => _projectedSpread - _bettingSpread;
    private double _totalEdge => _projectedTotal - _bettingTotal;
    
    private bool _isEnsembleLoading = false;
    private KyleskomEnsembleResult? _ensembleResult;
    
    // Save State
    private bool _isSaving = false;

    private async Task SaveToCache()
    {
        _isSaving = true;
        try
        {
            // Build a summary object to cache
            var analysisData = new Dictionary<string, object>
            {
                ["projected_total"] = _projectedTotal,
                ["projected_spread"] = _projectedSpread,
                ["total_edge"] = _totalEdge,
                ["spread_edge"] = _spreadEdge
            };
            
            if (_ensembleResult != null)
            {
                analysisData["win_prob"] = _ensembleResult.HomeWinProbability;
                analysisData["consensus_winner"] = _ensembleResult.PredictedWinner ?? Event.HomeTeam;
            }
            else if (_ncaabComparison != null)
            {
                var xgb = _ncaabComparison.GetXgbResult();
                if (xgb != null)
                {
                    analysisData["win_prob"] = xgb.WinProb;
                    analysisData["consensus_winner"] = xgb.Winner;
                    analysisData["total_proj"] = xgb.Total;
                }
            }
            
            var success = await MLService.SaveAnalysisAsync(Sport, Event.HomeTeam, Event.AwayTeam, analysisData);
            if (success)
            {
                Snackbar.Add("Analysis saved to daily cache!", Severity.Success);
            }
            else
            {
                Snackbar.Add("Failed to save analysis", Severity.Warning);
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error: {ex.Message}", Severity.Error);
        }
        finally
        {
            _isSaving = false;
        }
    }

    protected override async Task OnInitializedAsync()
    {
        await LoadStats();
        
        if (Sport == "basketball_nba")
        {
            await LoadEnsembleData();
        }
        else if (Sport == "basketball_ncaab")
        {
            await LoadNcaabComparisonData();
        }
    }

    private NcaabComparisonResponse? _ncaabComparison;

    private async Task LoadNcaabComparisonData()
    {
        try
        {
            _isEnsembleLoading = true;
            StateHasChanged();

            int? homeMl = (int?)Event.Bookmakers.FirstOrDefault()?.Markets.FirstOrDefault(m => m.Key == "h2h")?.Outcomes.FirstOrDefault(o => o.Name == Event.HomeTeam)?.Price;
            int? awayMl = (int?)Event.Bookmakers.FirstOrDefault()?.Markets.FirstOrDefault(m => m.Key == "h2h")?.Outcomes.FirstOrDefault(o => o.Name == Event.AwayTeam)?.Price;
            
            _ncaabComparison = await MLService.CompareNcaabModelsAsync(
                Event.HomeTeam, 
                Event.AwayTeam, 
                _bettingTotal,
                homeMl,
                awayMl);
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error loading NCAAB comparison: {ex.Message}", Severity.Warning);
        }
        finally
        {
            _isEnsembleLoading = false;
            StateHasChanged();
        }
    }

    private async Task LoadEnsembleData()
    {
        try
        {
            _isEnsembleLoading = true;
            StateHasChanged();

            int? homeMl = (int?)Event.Bookmakers.FirstOrDefault()?.Markets.FirstOrDefault(m => m.Key == "h2h")?.Outcomes.FirstOrDefault(o => o.Name == Event.HomeTeam)?.Price;
            int? awayMl = (int?)Event.Bookmakers.FirstOrDefault()?.Markets.FirstOrDefault(m => m.Key == "h2h")?.Outcomes.FirstOrDefault(o => o.Name == Event.AwayTeam)?.Price;
            
            var comparison = await MLService.CompareNbaModelsAsync(
                Event.HomeTeam, 
                Event.AwayTeam, 
                _bettingTotal,
                homeMl,
                awayMl);

            if (comparison != null)
            {
                _ensembleResult = comparison.GetKyleskomResult();
            }
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error loading NN predictions: {ex.Message}", Severity.Warning);
        }
        finally
        {
            _isEnsembleLoading = false;
            StateHasChanged();
        }
    }

    private async Task LoadStats()
    {
        try
        {
            _isLoading = true;
            _errorMessage = null;
            
            _homeId = await StatsService.GetTeamIdAsync(Event.HomeTeam, Sport);
            _awayId = await StatsService.GetTeamIdAsync(Event.AwayTeam, Sport);

            if (_homeId.HasValue && _awayId.HasValue)
            {
                // 2. Fetch Stats (Parallel)
                var homeTask = StatsService.GetTeamStatsAsync(_homeId.Value, Sport);
                var awayTask = StatsService.GetTeamStatsAsync(_awayId.Value, Sport);
                var h2hTask = StatsService.GetHeadToHeadAsync(_homeId.Value, _awayId.Value, Sport);

                await Task.WhenAll(homeTask, awayTask, h2hTask);
                
                _homeStats = await homeTask;
                _awayStats = await awayTask;
                _h2hGames = await h2hTask;
                
                if (_homeStats != null && _awayStats != null) 
                {
                    // 3. fetch top players from last game
                    // SKIP for NFL (API restricts player stats on free/standard key)
                    bool isNfl = Sport.Contains("nfl", StringComparison.OrdinalIgnoreCase) || Sport.Contains("americanfootball", StringComparison.OrdinalIgnoreCase);
                    
                    if (!isNfl)
                    {
                        if (_homeStats.LastGameId > 0)
                            _homeTopPlayers = (await StatsService.GetGameStatsAsync(_homeStats.LastGameId, Sport))
                                .Where(p => p.Team.Id == _homeId.Value)
                                .OrderByDescending(p => p.Pts)
                                .Take(3).ToList();

                        if (_awayStats.LastGameId > 0)
                            _awayTopPlayers = (await StatsService.GetGameStatsAsync(_awayStats.LastGameId, Sport))
                                .Where(p => p.Team.Id == _awayId.Value)
                                .OrderByDescending(p => p.Pts)
                                .Take(3).ToList();
                    }
                            
                    // 4. Calculate Projections
                    // Projected Home Score = (Home Avg Scored + Away Avg Allowed) / 2
                    var projHome = (_homeStats.AveragePoints + _awayStats.AveragePointsAllowed) / 2.0;
                    var projAway = (_awayStats.AveragePoints + _homeStats.AveragePointsAllowed) / 2.0;
                    
                    _projectedTotal = projHome + projAway;
                    _projectedSpread = projHome - projAway; // Positive = Home favored
                    
                    // 5. Extract betting lines from odds
                    ExtractBettingLines();
                }
                else
                {
                    _statsMissing = true;
                }
            }
            else
            {
                _statsMissing = true;
            }
        }
        catch (HttpRequestException ex) when (ex.StatusCode == System.Net.HttpStatusCode.TooManyRequests)
             {
                 _errorMessage = "Rate limit exceeded. Please wait a moment before trying again.";
                 Snackbar.Add("BallDontLie Rate Limit Hit", Severity.Warning);
             }
        catch (Exception ex)
        {
            _errorMessage = $"Error loading stats: {ex.Message}";
            Snackbar.Add($"Error loading stats: {ex.Message}", Severity.Error);
            _statsMissing = true;
        }
        finally
        {
            _isLoading = false;
        }
    }

    private void Cancel()
    {
        try
        {
            if (DialogInstance == null)
            {
                Console.WriteLine("DialogInstance is null");
                return;
            }

            var type = (Type)DialogInstance.GetType();
            Console.WriteLine($"DialogInstance Type: {type.FullName}");
            foreach (var method in type.GetMethods())
            {
                Console.WriteLine($"Method: {method.Name}");
            }

            // Attempt to call Close or Cancel if found
            if (type.GetMethod("Cancel") != null)
            {
                DialogInstance.Cancel();
            }
            else if (type.GetMethod("Close") != null)
            {
                 // Try Close with no args or default
                 try 
                 { 
                     DialogInstance.Close(); 
                 } 
                 catch 
                 { 
                     // Try with DialogResult.Cancel() if possible, but we need the type
                     // For now just logging what's available
                 }
            }
            else 
            {
                 Console.WriteLine("No Close or Cancel method found!");
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error in Cancel: {ex}");
        }
    }
    
    private string FormatPrice(double? price) => price.HasValue ? (price > 0 ? $"+{price}" : price.ToString()) : "-";
    
    private string FormatMarketKey(string key)
    {
        return key switch
        {
            "h2h" => "Moneyline",
            "spreads" => "Spread",
            "totals" => "Total",
            _ => key
        };
    }

    private void ExtractBettingLines()
    {
        // Extract spread and total from first available bookmaker
        var bookmaker = Event.Bookmakers.FirstOrDefault();
        if (bookmaker == null) return;

        // Extract spread (for home team)
        var spreadMarket = bookmaker.Markets.FirstOrDefault(m => m.Key == "spreads");
        if (spreadMarket != null)
        {
            var homeSpread = spreadMarket.Outcomes.FirstOrDefault(o => o.Name == Event.HomeTeam);
            if (homeSpread?.Point.HasValue == true)
            {
                _bettingSpread = -homeSpread.Point.Value; // Negate: -3.5 line means book expects home to win by 3.5
            }
        }

        // Extract total (O/U line)
        var totalMarket = bookmaker.Markets.FirstOrDefault(m => m.Key == "totals");
        if (totalMarket != null)
        {
            var overOutcome = totalMarket.Outcomes.FirstOrDefault(o => o.Name == "Over");
            if (overOutcome?.Point.HasValue == true)
            {
                _bettingTotal = overOutcome.Point.Value;
            }
        }
    }

    private Color GetEdgeColor(double spread)
    {
        // Simple heuristic: if spread > 3 or < -3, it's a "significant" projection
        if (Math.Abs(spread) > 7.0) return Color.Success; // Strong opinion
        if (Math.Abs(spread) > 3.0) return Color.Warning; // Moderate
        return Color.Default;
    }

    // Edge Meter Helper Methods
    private string GetEdgeMeterColor(double edge)
    {
        if (edge >= 3) return "#4caf50";  // Green - strong value
        if (edge >= 1) return "#8bc34a";  // Light green - some value
        if (edge <= -3) return "#f44336"; // Red - bad value
        if (edge <= -1) return "#ff9800"; // Orange - slight concern
        return "#9e9e9e";                 // Gray - neutral
    }

    private string GetEdgeDashArray(double edge)
    {
        // Arc length is ~125.6 (half circle with radius 40)
        // Map edge (-10 to +10) to arc fill percentage
        var arcLength = 125.6;
        var normalized = Math.Clamp(edge / 10.0, -1.0, 1.0); // -1 to 1
        var fillPercent = 50 + (normalized * 50); // 0 to 100
        var filled = arcLength * (fillPercent / 100.0);
        return $"{filled} {arcLength}";
    }

    private Color GetEdgeTextColor(double edge)
    {
        if (edge >= 3) return Color.Success;
        if (edge >= 1) return Color.Info;
        if (edge <= -3) return Color.Error;
        if (edge <= -1) return Color.Warning;
        return Color.Default;
    }

    private Color GetEdgeChipColor(double edge)
    {
        if (edge >= 3) return Color.Success;
        if (edge >= 1) return Color.Info;
        if (edge <= -3) return Color.Error;
        if (edge <= -1) return Color.Warning;
        return Color.Default;
    }

    private string GetEdgeLabel(double edge)
    {
        if (edge >= 5) return "STRONG VALUE";
        if (edge >= 2) return "VALUE";
        if (edge <= -5) return "AVOID";
        if (edge <= -2) return "NO VALUE";
        return "NEUTRAL";
    }

    // AI Analysis State
    private bool _isAiLoading = false;
    private AiAnalysisReport? _aiAnalysis;

    private async Task LoadAiAnalysis()
    {
        if (_aiAnalysis != null) return; // Already loaded

        _isAiLoading = true;
        try
        {
            // Simple sport mapping
            string simpleSport = "nba";
            if (Sport.Contains("nfl")) simpleSport = "nfl";
            else if (Sport.Contains("ncaab")) simpleSport = "ncaab";
            else if (Sport.Contains("nascar")) simpleSport = "nascar";
            
            _aiAnalysis = await MLService.AnalyzeMatchupAsync(simpleSport, Event.HomeTeam, Event.AwayTeam);
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error loading AI analysis: {ex.Message}", Severity.Warning);
        }
        finally
        {
            _isAiLoading = false;
            StateHasChanged(); // Re-render
        }
    }

    private string CleanseRationale(string? input)
    {
        if (string.IsNullOrEmpty(input)) return "";
        
        // Final fallback to strip markdown leftovers that the backend might have missed
        var cleansed = input.Replace("```html", "").Replace("```", "").Trim();
        
        // Ensure bolding is handled even if LLM ignores instructions
        cleansed = Regex.Replace(cleansed, @"\*\*(.*?)\*\*", "<b>$1</b>");
        cleansed = Regex.Replace(cleansed, @"\*(.*?)\*", "<i>$1</i>");
        
        // Remove literal quotes that might be wrapping the string
        if (cleansed.StartsWith("\"") && cleansed.EndsWith("\"")) cleansed = cleansed[1..^1];
        
        return cleansed;
    }

    private class RationaleSection { public string Title { get; set; } = ""; public string Content { get; set; } = ""; }

    private List<RationaleSection> SplitRationale(string? rationale)
    {
        var result = new List<RationaleSection>();
        if (string.IsNullOrEmpty(rationale)) return result;

        // Split by <h4> tags
        var matches = Regex.Matches(rationale, @"<h4>(.*?)</h4>(.*?)(?=<h4>|$)", RegexOptions.Singleline);
        foreach (Match m in matches)
        {
            result.Add(new RationaleSection 
            { 
                Title = CleanTitle(m.Groups[1].Value), 
                Content = m.Groups[2].Value.Trim() 
            });
        }

        if (!result.Any())
        {
            // Fallback if no <h4> found
            result.Add(new RationaleSection { Title = "Analysis", Content = rationale });
        }

        return result;
    }

    private string CleanTitle(string title)
    {
        // Remove leading numbers like "1. "
        return Regex.Replace(title, @"^\d+\.\s*", "").Trim();
    }

    private string GetSectionIcon(string title)
    {
        var t = title.ToLower();
        if (t.Contains("market")) return Icons.Material.Filled.ShowChart;
        if (t.Contains("injury")) return Icons.Material.Filled.Medication;
        if (t.Contains("matchup")) return Icons.Material.Filled.Compare;
        if (t.Contains("environment") || t.Contains("total")) return Icons.Material.Filled.Speed;
        if (t.Contains("situational")) return Icons.Material.Filled.Place;
        if (t.Contains("summary")) return Icons.Material.Filled.Assignment;
        return Icons.Material.Filled.Analytics;
    }
}
